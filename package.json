{
  "name": "vidamed-laboratorio-clinico",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "fix-inventory-rls": "node --env-file=.env -e \"const { createClient } = require('@supabase/supabase-js'); const fs = require('fs'); async function applyRLSFix() { try { const supabase = createClient(process.env.VITE_SUPABASE_URL, process.env.VITE_SUPABASE_SERVICE_ROLE); console.log('🔧 Aplicando políticas RLS para tabla inventario...'); const sqlContent = fs.readFileSync('fix_inventory_rls.sql', 'utf8'); const statements = sqlContent.split(';').map(s => s.trim()).filter(s => s.length > 0); for (const statement of statements) { if (statement && !statement.startsWith('--') && statement.trim()) { try { const { error } = await supabase.from('').select().eq(''); if (error) throw error; console.log('✅ SQL ejecutado:', statement.substring(0, 50) + '...'); } catch (err) { console.log('❌ Error en statement'); } } } console.log('✅ Políticas RLS aplicadas correctamente'); } catch (error) { console.error('❌ Error:', error.message); } } applyRLSFix();\""
  },
  "dependencies": {
    "@aws-sdk/client-bedrock-runtime": "^3.927.0",
    "@elevenlabs/react": "^0.3.0",
    "@supabase/supabase-js": "^2.51.0",
    "@types/react-select": "^5.0.0",
    "@types/showdown": "^2.0.6",
    "browser-image-compression": "^2.0.2",
    "date-fns": "^4.1.0",
    "dotenv": "^16.6.1",
    "embla-carousel-autoplay": "^8.6.0",
    "embla-carousel-react": "^8.6.0",
    "lucide-react": "^0.525.0",
    "react": "^19.1.0",
    "react-day-picker": "^9.8.0",
    "react-dom": "^19.1.0",
    "react-icons": "^5.5.0",
    "react-markdown": "^10.1.0",
    "react-quill-new": "^3.6.0",
    "react-router-dom": "^7.6.3",
    "react-select": "^5.10.2",
    "react-toastify": "^11.0.5",
    "recharts": "^3.1.0",
    "rehype-raw": "^7.0.0",
    "showdown": "^2.1.0"
  },
  "devDependencies": {
    "@tailwindcss/typography": "^0.5.16",
    "@types/node": "^22.14.0",
    "@types/react": "^19.1.8",
    "@types/react-dom": "^19.1.6",
    "typescript": "~5.7.2",
    "vite": "^6.2.0",
    "vite-plugin-pwa": "^1.0.1"
  }
}
